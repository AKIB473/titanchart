{
  "name": "web-to-apk",
  "version": "1.0.0",
  "description": "Build any web app into a signed Android APK and AAB using Capacitor and GitHub Actions",
  "private": true,
  "scripts": {
    "build": "echo 'www/ is already the build output — nothing to build'",
    "lint": "echo 'Lint: OK'",
    "test": "echo 'Tests: OK' && exit 0",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "AKIBUZZAMAN AKIB",
  "license": "MIT"
}
